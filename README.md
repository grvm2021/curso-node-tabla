# Notas.

Este es mi primer program en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar                  [number]
  -l, --listar   Muestra la tabla en consola                           [boolean]
  -h, --hasta    Indica hasta que numero se multiplica                  [number]
  
```